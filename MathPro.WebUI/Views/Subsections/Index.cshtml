@model IEnumerable<MathPro.Domain.Entities.Subsection>

@{
    ViewBag.Title = "Подразделы";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Создать новую запись", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Изменить", "Edit", new { id=item.SubsectionId }) |
            @Html.ActionLink("Подробнее", "Details", new { id=item.SubsectionId }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.SubsectionId })
        </td>
    </tr>
}

</table>
